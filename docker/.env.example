# ===========================================
# PD-OS Docker Environment Configuration
# ===========================================
# Copy this file to .env and modify as needed
# cp .env.example .env

# Database Configuration
DB_USER=pdos_user
DB_PASSWORD=pdos_password
DB_NAME=pdos_db
DB_PORT=5432

# Backend Configuration
BACKEND_PORT=3001
JWT_SECRET=change-this-to-a-secure-random-string-in-production
JWT_EXPIRES_IN=7d

# Frontend Configuration
FRONTEND_PORT=3000
